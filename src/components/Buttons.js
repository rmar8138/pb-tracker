import styled from "styled-components";

export const LogoutButton = styled.button`
  background-color: inherit;
  border: none;
  color: #f44336;
  cursor: pointer;
  font-family: inherit;
  font-size: inherit;
  margin: 0;
  padding: 0;
  transition: all 0.2s;

  :hover {
    color: #ca392f;
  }
`;
